import 'package:flutter/material.dart';

class JoinedCompetitionPageScreen extends StatelessWidget {
  const JoinedCompetitionPageScreen({super.key});

  @override
  Widget build(context) {
    return const Center(
      child: Text('Joined Competition page'),
    );
  }
}
